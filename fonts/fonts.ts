/**
 * This file is used for storing fonts.
 */

import { Inter, Poppins } from "next/font/google";

export const titleFont = Inter({
    weight: "700",
    subsets: ["latin"],
})

export const secondaryFont = Poppins({
    weight: "400",
    subsets: ["latin"],
})
